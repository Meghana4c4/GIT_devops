# devops GIT
```
sudo apt-get update
sudo apt install git
git --version
mkdir git
ls
cd git
vim file.txt
ls
git init
git status
git add file.txt
git commit -m "demo"file.txt
git checkout -b main
git remote -v (incase of error)
git remote remove origin (incase of error)
git remote add origin https://github.com/Meghana4c4/devops.git
git push origin master
```
